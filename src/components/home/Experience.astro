---
import { IExperiences } from "@types";

const { title, details } = Astro.props as IExperiences;
---

<section
  class="flex flex-col items-start justify-between sm:flex-row dark:text-light"
>
  <h2 class="mt-16 w-60">{title}</h2>
  <div class="w-full">
    {
      details.map((item) => (
        <div class="my-16">
          <div class="flex flex-wrap items-center justify-between">
            <h3 class="text-xl font-semibold">
              {item.name} - <span class="text-blue">{item.company}</span>
            </h3>
            <p class="text-sm dark:text-gray">
              {item.startDate} - {item.endDate}
            </p>
          </div>
          <p class="mt-3 text-sm text-blue">{item.location}</p>
          <ul class="mt-3">
            {item.description.map((item) => (
              <li class="dark:text-slate-200">{item}</li>
            ))}
          </ul>
        </div>
      ))
    }
  </div>
</section>
